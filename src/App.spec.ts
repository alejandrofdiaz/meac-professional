// import { App } from './App';

describe('App', () => {
  // eslint-disable-next-line jest/no-disabled-tests
  it.skip('should return hello world', () => {
    console.log('pending to implement testing library');
  });
});
